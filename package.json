{
  "name": "@cycle/history",
  "version": "1.1.0",
  "description": "Cycle.js Driver based on the rackt/history library",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cyclejs/cycle-history.git"
  },
  "keywords": [
    "cycle",
    "cyclejs",
    "reactive",
    "history",
    "driver"
  ],
  "author": "Tylor Steinberger <tlsteinberger167@gmai.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cyclejs/cycle-history/issues"
  },
  "homepage": "https://github.com/cyclejs/cycle-history#readme",
  "dependencies": {
    "history": "^1.x.x"
  },
  "peerDependencies": {
    "rx": "4.x.x"
  },
  "devDependencies": {
    "assert": "^1.3.0",
    "babel-cli": "^6.4.5",
    "babel-core": "^6.4.5",
    "babel-eslint": "^4.1.6",
    "babel-plugin-syntax-object-rest-spread": "^6.3.13",
    "babel-plugin-transform-object-rest-spread": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "eslint": "^1.10.3",
    "eslint-config-cycle": "^3.2.0",
    "eslint-plugin-cycle": "^1.0.2",
    "eslint-plugin-no-class": "^0.1.0",
    "mocha": "^2.3.4",
    "rx": "^4.0.7",
    "testem": "^0.9.11",
    "uglify-js": "^2.6.1"
  },
  "scripts": {
    "eslint": "eslint src/",
    "test": "npm run eslint && testem",
    "test-ci": "testem ci",
    "prebrowserify": "mkdir dist/",
    "browserify": "browserify src/index.js -t babelify --standalone cycle-history -o dist/cycle-history.js",
    "uglify": "uglifyjs dist/cycle-history.js -o dist/cycle-history.min.js",
    "precompile-lib": "rm -rf lib/ && mkdir -p lib",
    "compile-lib": "babel -d lib/ src/",
    "predist": "rm -rf dist/",
    "dist": "npm run browserify && npm run uglify",
    "prepublish": "npm run compile-lib",
    "release": "npm run release-patch",
    "release-patch": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version patch && git push origin master --tags && npm publish --access=public",
    "release-minor": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version minor && git push origin master --tags && npm publish --access=public",
    "release-major": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version major && git push origin master --tags && npm publish --access=public"
  }
}
